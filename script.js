const myButton = document.getElementById("incorrect");
const body =document.body;
const colors = [ 'red']

button.addEventListener('click', chageBackground)
function changeBackground(){
  body.style.backgroundColor = colors[colorsIndex]
}